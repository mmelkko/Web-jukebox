<%= form_for(@play_queue) do |f| %>
  <% if @play_queue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@play_queue.errors.count, "error") %> prohibited this play_queue from being saved:</h2>

      <ul>
      <% @play_queue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room %><br />
    <%= f.text_field :room %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
